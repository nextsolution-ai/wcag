{
  "name": "wcag-accessibility-widget",
  "version": "1.0.0",
  "description": "A customizable accessibility widget that helps make websites WCAG compliant",
  "main": "dist/widget.min.js",
  "scripts": {
    "build:css": "cleancss -o dist/style.min.css src/style.css && cleancss -o dist/profiles.min.css src/profiles.css",
    "build:js": "uglifyjs src/widget.js -o dist/widget.min.js",
    "build": "npm run build:css && npm run build:js"
  },
  "keywords": [
    "accessibility",
    "wcag",
    "widget",
    "a11y"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "uglify-js": "^3.19.3"
  }
}
